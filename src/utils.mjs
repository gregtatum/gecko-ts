// @ts-check

/**
 * Log something to console.log.
 *
 * @param {string} input
 */
export function log(input) {
  console.log(input);
}

/**
 * Example function that would add two numbers.
 *
 * @param {number} a
 * @param {number} b
 */
 export function add(a, b) {
  return a + b;
}
